import React from 'react';
import styles from "./styles.module.css";

function Components() {
  return(
    <div className={styles.header}>
      <h1 className={styles.headerText}>Mid Test</h1>
    </div>);
}

export default Components;